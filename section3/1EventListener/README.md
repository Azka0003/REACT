# React Event Handling Demo

This is a simple React project that demonstrates how **event handling** works in React using `onClick`, `onMouseOver`, and `onMouseOut`.

The app shows an image and a button.  
When you interact with them, different events are triggered.

---

## 🚀 Features

- Logs a message when the button is clicked
- Adds a red border when the image is hovered
- Removes the border when the mouse leaves the image

---

## 🧠 Concepts Used

- React functional components
- Event handling in React
- `onClick`, `onMouseOver`, `onMouseOut`
- DOM manipulation using `event.target`

---

## 📷 What Happens?

- Hover on the image → red border appears  
- Move mouse away → border disappears  
- Click the button → `"I was clicked!"` appears in console

---

## 🛠️ Tech Stack

- React
- JavaScript
- JSX

---

## ▶️ How to Run

1. Install dependencies  
   ```bash
   npm install
